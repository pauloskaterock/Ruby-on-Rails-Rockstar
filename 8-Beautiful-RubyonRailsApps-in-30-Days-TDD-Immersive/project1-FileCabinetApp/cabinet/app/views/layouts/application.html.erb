<!DOCTYPE html>
<html>
  <head>
    <title>FileCabinet | Your life's work!</title>

    <!-- Inclui o CSS principal da aplicação -->
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'true' %>

    <!-- Inclui o JavaScript principal da aplicação -->
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'true' %>

    <!-- Tags de segurança para formulários -->
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <div class="header_inner">
        <!-- Link para a página inicial com ID 'logo' -->
        <%= link_to "FileCabinet", root_path, id: "logo" %>

        <nav>
          <% if user_signed_in? %>
            <!-- Link para criar novo documento -->
            <%= link_to "Create Doc", new_doc_path %>

            <!-- Link para sair da sessão (DELETE) -->
            <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
          <% else %>
            <!-- Link para login -->
            <%= link_to "Log In", new_user_session_path %>
          <% end %>
        </nav>
      </div>
    </header>

    <!-- Mensagens de notificação e alerta -->
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <!-- Renderiza o conteúdo da view atual -->
    <%= yield %>
  </body>
</html>
