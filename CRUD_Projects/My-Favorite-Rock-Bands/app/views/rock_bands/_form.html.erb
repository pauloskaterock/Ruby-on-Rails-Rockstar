<%= form_with(model: rock_band) do |form| %>
  <% if rock_band.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rock_band.errors.count, "error") %> prohibited this rock_band from being saved:</h2>

      <ul>
        <% rock_band.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_field :text %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
